version: "3"

services:
    client:
        image: nginx
        ports:
            - 8000:80
        volumes:
            - nginx:/usr/share/nginx
            
volumes:
  nginx:
    external: true
    name: "nginx"        
