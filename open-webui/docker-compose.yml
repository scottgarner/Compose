version: '3'
services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: always
    ports:
      - "4000:8080"
    volumes:
      - open-webui:/app/backend/data
volumes:
  open-webui: /mnt/docker/open-webui
